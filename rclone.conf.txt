# ========================================================
# OPTION 1: PRIMARY - Direct Terabox API (Recommended)
# ========================================================
# This bypasses WebDAV for more direct & reliable access
# Requires: TERABOX_ACCESS_TOKEN or TERABOX_EMAIL/PASSWORD in env
# 
# To configure:
# 1. Get access token from Terabox
# 2. Set: export TERABOX_ACCESS_TOKEN=your_token
# 3. Or set: TERABOX_EMAIL and TERABOX_PASSWORD in .env
#
# [terabox-direct]
# type = baidu
# app_id = 250528
# app_secret = [leave blank for now]
# access_token = [auto-managed by TeraboxDirectAPI]
# refresh_token = [cached locally]

# ========================================================
# OPTION 2: FALLBACK - Terabox via Alist WebDAV
# ========================================================
# Use this if direct API doesn't work
# Requires: Alist running on localhost:5244
[terabox]
type = webdav
url = http://localhost:5244/dav/terabox
vendor = other
user = admin
pass = nOBxHSEklm1M-QWIKlzw_93rRHRwZ16b
# Note: Token auto-refreshed by TeraboxCredentialManager
# This ensures cookie/auth is always fresh

# ========================================================
# ENCRYPTION: Terabox with client-side encryption
# ========================================================
[terabox_crypt]
type = crypt
remote = terabox:/arsip_encrypted
filename_encryption = standard
directory_name_encryption = true
password = uR-oRsbNnnKcfycXNO_4o4i5luHbnE-ncDCN3JaRvC4

# ========================================================
# CACHE: Local cache for frequently accessed files
# ========================================================
[terabox_cache]
type = cache
remote = terabox:/
plex_token = 
chunk_size = 5M
db_path = ./cache/terabox.db
chunk_path = ./cache/chunks
db_wait_time = 1m
chunk_total_size = 1G

# ========================================================
# BACKUP: Google Drive (add credentials when ready)
# ========================================================
[gdrive]
type = drive
client_id = 
client_secret = 
scope = drive
root_folder_id = 
token = 
# Use when needed: rclone sync terabox: gdrive:backup

# ========================================================
# ARCHIVE: Backblaze B2 (add credentials when ready)
# ========================================================
[b2]
type = b2
account_id = 
app_key = 
# Use for monthly archive: rclone sync terabox: b2:archive

# ========================================================
# DECENTRALIZED: Storj (optional)
# ========================================================
[storj]
type = s3
provider = other
env_auth = false
access_key_id = dummy
secret_access_key = dummy
endpoint = https://gateway.storjshare.io
